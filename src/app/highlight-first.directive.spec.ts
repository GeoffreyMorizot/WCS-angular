/* import { HighlightFirstDirective } from './highlight-first.directive';

describe('HighlightFirstDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightFirstDirective();
    expect(directive).toBeTruthy();
  });
});
 */